import React from 'react';

import Base from './Base';

export default class BackgroundShader extends Base {
    render() {
        return (
            <div className='BackgroundShader'/>
        );
    }
}

